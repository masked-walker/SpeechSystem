<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.nullcat.sckj.mapper.BookingMapper">

    <select id="findConflictBookings" resultType="cn.nullcat.sckj.pojo.Booking">
        SELECT *
        FROM booking
        WHERE room_id = #{roomId}
        AND status = 1 <!-- 已通过的预订 -->
        AND is_deleted = 0
        AND (
            (start_time &lt;= #{startTime} AND end_time &gt; #{startTime}) OR
            (start_time &lt; #{endTime} AND end_time &gt;= #{endTime}) OR
            (start_time &gt;= #{startTime} AND end_time &lt;= #{endTime})
        )
    </select>
</mapper>